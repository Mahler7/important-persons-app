<h1> <%= @event.name %></h1>
<img src="<%= @image %>" class="img-responsive">
<p> <%= @event.date %> <%= @event.reminder %></p>
<p> <%= @event.description %></p>
<div>
  <%= link_to "Edit", "/events/#{@event.id}/edit" %>
  <%= link_to "Delete", "/events/#{@event.id}" %>
</div>